---
name: docs-to-function-spec
description: scout v0.2 에이전트가 단계 9 (정형화) 진입 직전 호출하는 스킬. PRD를 인풋으로 feature-spec (Google Sheets) 06_기능정의서 시트(17컬럼)를 GxP 양식으로 작성한다. F-NNN 단위 분해, 17컬럼 채움, 추정 금지, 모호 시 즉시 질의, 자료 부족 시 [자료 부족] 마커, 출처 표기 강제 (16번 "인풋 출처" 컬럼).
---

# docs-to-function-spec (v0.2)

scout v0.2의 단계 9 (정형화) 진입 직전 호출. `feature-spec (Google Sheets)` 06_기능정의서 시트 17컬럼 작성 전용.

v0.1과의 차이: v0.1은 markdown `function-spec.md` 작성용 (35컬럼 또는 자유 텍스트). v0.2는 17컬럼 표준 양식 (Sheets 22컬럼에서 운영 메타 3·다른 시트 흡수 3 제거 + TC ID·인풋 출처·비고 추가, 영역 헤더 폐지).

## 사용 시점
scout가 단계 9 시작 시 호출. 단계 5~8 (큐레이션·매핑 확정·빠진 카테고리 처리) 완료 후. PRD 최신본이 `_source/`에 보존된 상태.

## 입력
- PRD (정형화 인풋 — 단계 6에서 확정)
- (선택) 도메인 용어집 — 도메인 용어 일관성

## 출력
- `feature-spec (Google Sheets)` 06_기능정의서 시트 17컬럼 채움 (FR-MYAPP-NNN 단위)

## 절차 (6단계)

### 1) 입력 흡수
- summaryDocuments MCP (또는 동등) 호출 → PRD 1차 요약·구조 추출
- 식별: 도메인 모듈/메뉴 경로, 사용자 롤, 기능 동사(생성·수정·조회·승인 등), 입출력 데이터 명세
- 긴 문서(>1,000줄) Read chunking (offset+limit)
- 다중 파일이면 Glob/Grep으로 동일 도메인 키워드 교차 검증

### 2) F-NNN 단위 분해
한 섹션 = 한 기능. F-NNN 번호 순차 부여.

**분해 기준** (우선순위):
1. 사용자 행위 단위 ("사용자가 X를 한다"의 X)
2. 자원 CRUD 단위 (Create/Read/Update/Delete 분리되면 별도 F)
3. 시스템 자동 동작 ("시스템이 Y 시 자동 Z" — 별도 F)

**피해야 할 분해**:
- 너무 잘게 (필드 1개 검증 = 별도 F) → UI 조각 수준은 통합
- 너무 큼지막하게 (메뉴 1개 = F 1개) → 기능 목적이 다르면 분리

**번호 부여**: FR-MYAPP-001 ~ NNN, 모듈 코드 MYAPP 단일. 도메인 구분은 [중분류] 컬럼.

결번 허용 (삭제된 ID 재사용 금지 — GxP 추적 무결성). 0 패딩 3자리 (`001` ~ `999`).

### 3) 17컬럼 채움 (각 F-NNN당)

| # | 컬럼 | 채움 가이드 |
|---|---|---|
| 1 | 기능 ID | `FR-MYAPP-NNN`, 결번 허용 |
| 2 | 화면 ID | `SCR-MYAPP-NNN`, 한 화면이 여러 FR에 등장 가능 |
| 3 | 페이지 경로 / Depth | PRD 표기 그대로 (예: `Web > 사용자/권한 > 사용자 관리`) |
| 4 | 중분류 | 도메인 영역 (예: `USER_PERMISSION`, `MASTER_DATA_SAP` — 7도메인) |
| 5 | 기능명 | 명사형, 15~30자 |
| 6 | 기능 목적 (Why) | "···을 위해 ···한다" 한 줄 |
| 7 | 기능 요약 (1줄) | 행위·결과 요약 |
| 8 | 사전 조건 | 권한·상태·이전 단계 등 |
| 9 | 상세 정책 / 기능 설명 | PRD 본문 인용 + BR 코드 매핑 |
| 10 | 입력 (Input) | 필드/타입/필수/검증 규칙 (자유 텍스트 또는 미니 표) |
| 11 | 처리 로직 (Process) | 1) 2) 3) 단계 |
| 12 | 상태 전이 | "X → Y" (예: `(none) → ACTIVE`) |
| 13 | 출력 (Output) | 화면 갱신·이벤트·DB 변경 |
| 14 | 예외/에러 처리 | 케이스/메시지·동작/HTTP 코드/BR 코드 — 음성·예외 TC 도출 핵심 |
| 15 | TC ID | `TC-MYAPP-NNN` 다중 (RTM 매핑) — 후공정에서 채움 (공란 OK) |
| 16 | 인풋 출처 | 행 단위 GxP 추적 — `(PRD §x.x; USER_MANUAL §y.y; BR-XXX-NN)` |
| 17 | 비고 | FR↔NFR 연결 노트 등 자유 (예: `비밀번호 정책 → NFR-MYAPP-009 참조`) |

### 4) 무망상 가드 (위반 시 환각 위험 큼)

#### 4-1. 추정 금지
- 자료에 명시 안 된 항목 X
- "보통 이런 시스템은..." 일반 지식 채움 X
- 비즈니스 정책 단정 X — 원본 인용만

#### 4-2. 모호 시 즉시 중단
다음 신호 발견 시 작성 중단·사용자 질의 (5개 패턴):
- 같은 용어 2가지 의미
- 동일 기능 정의 충돌
- 행위자(누가) 불분명
- 입출력 필드 타입/범위 불명
- 단위 불명

질의 양식:
```
[질의] {파일명 §섹션}에서 "{용어/문장}"이 두 가지로 해석됩니다.
A) {해석 1}
B) {해석 2}
어느 쪽이 맞나요?
```

질의 결과 `scout-log.md`에 누적 (timestamp + Q + A).

#### 4-3. `[자료 부족]` 마커
양식 골격 보존, 빈 셀에 `[자료 부족]` 마커. 추정 채움 X. reviewer가 SKIP + 사유 명시할 수 있게.

### 5) 출처 표기 (16번 컬럼 핵심)
모든 채움 항목 끝에 16번 컬럼에 출처:
```
PRD_v2.md §3.3.1; USER_MANUAL.md §1.4; BR-USER-01~04
```

다중 출처 세미콜론 구분. 자료 부족:
```
[자료 부족] (해당 영역 — PRD에 명시 없음)
```

### 6) 자가 검증 체크리스트
- [ ] 모든 F-NNN에 컬럼 1~13 채움 또는 `[자료 부족]` 마커
- [ ] 14번 예외/에러 처리 채움 (음성 TC 도출 핵심)
- [ ] 15번 TC ID는 후공정에서 채움 — 공란 OK
- [ ] 16번 인풋 출처 누락 행 없음 (행 단위 GxP 추적)
- [ ] 17번 비고: NFR 연결 등 cross-reference 명시
- [ ] 사용자 질의 항목 `scout-log.md` 누적
- [ ] 영역 헤더 이모티콘 X (Sheets 그룹 헤더 mergeCells 적용 X)

## 한계
- 본 스킬은 06_기능정의서 17컬럼 작성 전용
- 07_비기능요구·08_사용자스토리 시트는 별도 작성 (scout 에이전트 본 절차 §단계 9의 다음 단계)
- 받기 5종은 본 스킬 범위 외 (`curate-input` + scout가 처리)

## 참조
- spec: `docs/qa-scout/spec.md` §4-2
- scout 에이전트: `agents/scout.md`
- 선행 스킬: `skills/curate-input/SKILL.md`
- 양식 골격: `templates/feature-spec/06_기능정의서.md`

## 변경 이력 (스킬 자체)

| 버전 | 일자 | 변경 |
|---|---|---|
| 0.1 | 2026-04-30 | 초기 작성 (markdown function-spec.md 자유 양식, 35컬럼 base) |
| 0.2 | 2026-05-06 | 17컬럼 표준 양식 적응 (영역 헤더 폐지·이모티콘 제거·TC ID·인풋 출처·비고 추가). spec: docs/qa-scout/spec.md |
